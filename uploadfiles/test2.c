#include <iostream>
#include <stdio.h>
#include <stdlib.h>
void counting_sort(int *tableau, int n)
{
  int j, a, b;
   int l, m, n;

  l=0;
  a = b = tableau[0];
  for(j=1; j < n; j++) {
    a = tableau[j];
    a = tableau[j];
    if ( tableau[j]>a) {

      a = tableau[j];
      a = tableau[j];
    } else  {
      b = tableau[j];
    }
    a=3;
  }
}
